name: Core hook startup measurement

on:
  pull_request:
    branches: [main]
    paths:
      - "plugins/codexy/hooks/codexy-*.sh"
      - "plugins/codexy/hooks/codexy-*.cmd"
      - "plugins/codexy/hooks/codexy-child-thread-creation.py"
      - "plugins/codexy/hooks/codexy-subagent-ownership.py"
      - "plugins/codexy/hooks/codexy-thread-delivery.py"
      - ".github/workflows/core-hook-startup-measurement.yml"
      - "scripts/measure-core-hook-startup.ps1"
  workflow_dispatch:
    inputs:
      baseline_sha:
        description: Exact baseline commit to compare
        required: true
        default: 9f8c9e207054294b834ef621ae80eb4a8c963793
        type: string
      head_sha:
        description: Exact current commit to compare
        required: true
        type: string

run-name: Core hook startup ${{ github.event_name == 'workflow_dispatch' && inputs.baseline_sha || '9f8c9e207054294b834ef621ae80eb4a8c963793' }} vs ${{ github.event_name == 'workflow_dispatch' && inputs.head_sha || github.event.pull_request.head.sha }}

env:
  MEASUREMENT_BASELINE_SHA: ${{ github.event_name == 'workflow_dispatch' && inputs.baseline_sha || '9f8c9e207054294b834ef621ae80eb4a8c963793' }}
  MEASUREMENT_HEAD_SHA: ${{ github.event_name == 'workflow_dispatch' && inputs.head_sha || github.event.pull_request.head.sha }}

permissions:
  contents: read

jobs:
  measure:
    name: ${{ matrix.revision }} / ${{ matrix.concern }} / ${{ matrix.event }}
    runs-on: windows-latest
    timeout-minutes: 5
    strategy:
      fail-fast: false
      matrix:
        revision: [baseline, current]
        concern: [child, subagent, thread]
        event: [PreToolUse, PermissionRequest]
    steps:
      - name: Checkout measurement controller
        uses: actions/checkout@v7
        with:
          ref: ${{ env.MEASUREMENT_HEAD_SHA }}
          path: .measurement-controller
          fetch-depth: 1
          persist-credentials: false
      - uses: actions/checkout@v7
        with:
          ref: ${{ matrix.revision == 'baseline' && env.MEASUREMENT_BASELINE_SHA || env.MEASUREMENT_HEAD_SHA }}
          path: .measurement-repo
          fetch-depth: 1
          persist-credentials: false
      - name: Verify exact measurement inputs, controller, and checkout
        shell: pwsh
        env:
          BASELINE_SHA: ${{ env.MEASUREMENT_BASELINE_SHA }}
          HEAD_SHA: ${{ env.MEASUREMENT_HEAD_SHA }}
          EXPECTED_SHA: ${{ matrix.revision == 'baseline' && env.MEASUREMENT_BASELINE_SHA || env.MEASUREMENT_HEAD_SHA }}
          REVISION: ${{ matrix.revision }}
        run: |
          $controller = (Resolve-Path -LiteralPath ".measurement-controller").Path
          $repo = (Resolve-Path -LiteralPath ".measurement-repo").Path
          $shaPattern = '^[0-9a-f]{40}$'
          if ($env:BASELINE_SHA -notmatch $shaPattern -or $env:HEAD_SHA -notmatch $shaPattern) {
            throw "baseline_sha and head_sha must be lowercase full commit SHAs"
          }
          if ($env:BASELINE_SHA -eq $env:HEAD_SHA) { throw "baseline_sha and head_sha must differ" }
          if ($env:REVISION -notin @("baseline", "current")) { throw "unexpected revision" }
          $controllerActual = (git -C $controller rev-parse HEAD).Trim().ToLowerInvariant()
          if ($LASTEXITCODE -ne 0 -or $controllerActual -ne $env:HEAD_SHA) {
            throw "controller checkout mismatch: expected $env:HEAD_SHA, got $controllerActual"
          }
          if (git -C $controller status --porcelain) { throw "measurement controller is not clean" }
          $actual = (git -C $repo rev-parse HEAD).Trim().ToLowerInvariant()
          if ($LASTEXITCODE -ne 0 -or $actual -ne $env:EXPECTED_SHA) {
            throw "checkout identity mismatch: expected $env:EXPECTED_SHA, got $actual"
          }
          if (git -C $repo status --porcelain) { throw "measurement checkout is not clean" }
      - name: Measure native production hook launcher
        shell: pwsh
        env:
          EXPECTED_SHA: ${{ matrix.revision == 'baseline' && env.MEASUREMENT_BASELINE_SHA || env.MEASUREMENT_HEAD_SHA }}
          REVISION: ${{ matrix.revision }}
          CONCERN: ${{ matrix.concern }}
          EVENT_NAME: ${{ matrix.event }}
          OUTPUT_PATH: ${{ runner.temp }}\codexy-core-hook-measurement\results.jsonl
        run: |
          & .measurement-controller/scripts/measure-core-hook-startup.ps1 `
            -RepoRoot (Resolve-Path -LiteralPath ".measurement-repo").Path `
            -ExpectedSha $env:EXPECTED_SHA `
            -Revision $env:REVISION `
            -Concern $env:CONCERN `
            -EventName $env:EVENT_NAME `
            -OutputPath $env:OUTPUT_PATH
      - name: Upload measurement evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: core-hook-startup-${{ matrix.revision }}-${{ matrix.concern }}-${{ matrix.event }}-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}\codexy-core-hook-measurement\results.jsonl
          if-no-files-found: warn
          retention-days: 14
