interface:
  display_name: "Skill Evaluation"
  short_description: "Run private semantic, boundary, and cost evaluations for shipped skills."
  default_prompt: "You MUST use $skill-evaluation to evaluate a shipped skill with private cases without exposing holdout answers."
policy:
  allow_implicit_invocation: true
