interface:
  display_name: "Plugin Marketplace Prep"
  short_description: "Validate plugin manifests, marketplace entries, and assets."
  default_prompt: "You MUST use $plugin-marketplace-prep to verify this Codex plugin is marketplace-ready."
policy:
  allow_implicit_invocation: true
