interface:
  display_name: "Task Classification"
  short_description: "Classify Codexy work before setup, routing, or edits."
  default_prompt: "You MUST use $task-classification first to render one ordered two-column GFM table with exactly these eight rows: Lane type, Secondary surfaces, Owner decision, Atomic scope, Required skills, Required tools/evidence, First allowed action, Stop/blocker; you MUST complete it before Codexy setup, delegation, implementation, PR, review-response, or merge work begins."
policy:
  allow_implicit_invocation: true
