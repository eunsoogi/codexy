interface:
  display_name: "Task Classification"
  short_description: "Classify Codexy work before setup, routing, or edits."
  default_prompt: "Use $task-classification first to name the lane type, owner decision, atomic scope, required skills, required tools/evidence, first allowed action, and blocker before Codexy setup, delegation, implementation, PR, review-response, or merge work begins."
policy:
  allow_implicit_invocation: true
