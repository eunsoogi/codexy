interface:
  display_name: "QA"
  short_description: "Verify claims through the real surface."
  default_prompt: "Run $task-classification before validation or QA work, then use $qa to turn acceptance criteria into observable QA scenarios and evidence."
policy:
  allow_implicit_invocation: true
