---
name: orchestration
description: MUST use first to classify work and load only applicable authorities.
---

# Orchestration

The invoking task is root; a child owns only its delegated lane.
`references/context-tiers.json` is the sole routing and safety authority.
Unknown, incomplete, invalid, permission-sensitive, destructive, or unavailable
authority MUST fail closed.

Read the request, issue or PR, and `AGENTS.md`. Ask one question only when
unresolved intent changes scope, owner, risk, or external state. Otherwise
classify one task plus applicable surfaces and risk, resolve ordered routes,
deduplicate in order, and read only that union.

Keep current owner, scope, head, proof, blocker, and one next action; current
authority wins after compaction. Missing proof MUST NOT grant permission, and a
child MUST NOT gain parent authority. Clear work needs no table, N/A fields,
question, or tool receipt. Omit unrelated references, conversations, tool
bodies, and agent trees. Selected authorities own details.
Controlled proof MUST keep zero P0/P1 misses and MUST reject incomplete,
unavailable, or synthesized evidence.
