{
  "schema": "codexy.review-economics-lane-input.v1",
  "id": "security",
  "version": "1.0.0",
  "kind": "security",
  "profile": "strict",
  "seeds": [{ "id": "seed-p0-authz" }],
  "request": {
    "task": "Review a security-sensitive policy change with a seeded authorization bypass.",
    "scope": "policy and execution boundary",
    "expected_profile_behavior": "codexy-sentinel with xhigh reasoning",
    "required_findings": ["seed-p0-authz"]
  }
}
