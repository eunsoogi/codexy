{
  "schema": "codexy.review-economics-seed.v1",
  "id": "seed-p0-authz",
  "version": "1.0.0",
  "severity": "p0",
  "payload": {
    "task": "A review-control change must not allow an untrusted caller to bypass the required strict review profile.",
    "fixture": "caller-supplied reviewer identity and profile are not authoritative",
    "expected_behavior": "reject the bypass and retain the strict policy"
  },
  "expected_findings": [
    { "id": "seed-p0-authz", "severity": "p0", "required": true }
  ]
}
