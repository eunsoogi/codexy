{
  "schema": "codexy.review-economics-seed.v1",
  "id": "seed-p1-regression",
  "version": "1.0.0",
  "severity": "p1",
  "payload": {
    "task": "A review-response repair must preserve the original blocking finding and prove the replacement head was rechecked.",
    "fixture": "response omits the prior finding while reporting a successful repair",
    "expected_behavior": "retain the finding or reject the response as incomplete"
  },
  "expected_findings": [
    { "id": "seed-p1-regression", "severity": "p1", "required": true }
  ]
}
