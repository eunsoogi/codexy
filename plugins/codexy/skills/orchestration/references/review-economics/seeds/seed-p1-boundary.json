{
  "schema": "codexy.review-economics-seed.v1",
  "id": "seed-p1-boundary",
  "version": "1.0.0",
  "severity": "p1",
  "payload": {
    "task": "A review-economics package must reject an artifact path that escapes its declared package boundary.",
    "fixture": "lane input path contains an unsafe parent traversal",
    "expected_behavior": "fail closed before accepting the package"
  },
  "expected_findings": [
    { "id": "seed-p1-boundary", "severity": "p1", "required": true }
  ]
}
