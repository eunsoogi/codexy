interface:
  display_name: "Debugging"
  short_description: "Reproduce, isolate, fix, and prove regressions."
  default_prompt: "Use $debugging to reproduce this failure, test hypotheses, and verify the smallest fix."
policy:
  allow_implicit_invocation: true
