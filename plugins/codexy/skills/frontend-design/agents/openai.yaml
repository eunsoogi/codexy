interface:
  display_name: "Frontend Design"
  short_description: "Design and verify polished user-facing UI."
  default_prompt: "You MUST use $frontend-design to align this UI with product context, expected states, and visual QA evidence."
policy:
  allow_implicit_invocation: true
