interface:
  display_name: "Dreaming"
  short_description: "MUST clean up compacted Codexy context before continuing."
  default_prompt: "You MUST use $dreaming after compaction or noisy handoff to refresh current anchors, MUST separate Remember, Fix, and Forget or demote items, and MUST prevent resolved review feedback or stale branch state from being carried forward as active work."
policy:
  allow_implicit_invocation: true
