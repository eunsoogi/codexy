interface:
  display_name: "Git Workflow"
  short_description: "Use Codexy issue, PR, review, and squash-merge rules."
  default_prompt: "You MUST use $git-workflow to run this repository change through issue, worktree, PR, Codex review, squash merge, and main sync."
policy:
  allow_implicit_invocation: true
