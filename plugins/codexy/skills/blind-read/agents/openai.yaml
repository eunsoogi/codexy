interface:
  display_name: "Blind Read"
  short_description: "Test whether a fresh reader can understand and act on one artifact."
  default_prompt: "You MUST use $blind-read to interpret one artifact for its intended audience and action without outside context."
policy:
  allow_implicit_invocation: true
