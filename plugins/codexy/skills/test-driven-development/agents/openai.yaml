interface:
  display_name: "Test-Driven Development"
  short_description: "Use RED-GREEN-REFACTOR with faithful proofs."
  default_prompt: "You MUST use $test-driven-development to choose a failing proof, implement the smallest change, and MUST verify GREEN."
policy:
  allow_implicit_invocation: true
