interface:
  display_name: "Wiki"
  short_description: "Use LLM Wiki methodology for durable project knowledge."
  default_prompt: "You MUST use $wiki to initialize, MUST ingest, MUST query, MUST compile, MUST audit, MUST capture, or MUST rehydrate topic-scoped LLM Wiki knowledge with provenance."
policy:
  allow_implicit_invocation: true
