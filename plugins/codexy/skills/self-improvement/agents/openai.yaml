interface:
  display_name: "Self-Improvement"
  short_description: "Choose bounded improvements from task evidence."
  default_prompt: "You MUST use $self-improvement to select only useful improvements for this task while preserving its goal, success criteria, authority, and finite budget."
policy:
  allow_implicit_invocation: true
