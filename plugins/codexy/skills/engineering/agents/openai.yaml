interface:
  display_name: "Engineering"
  short_description: "One atomic workflow for diagnosis, specification, modeling, testing, refactoring, and QA."
  default_prompt: "You MUST run $task-classification before engineering work, then MUST use $engineering to choose the applicable diagnosis, specification, domain modeling, TDD, refactoring, and QA sections for this atomic engineering outcome."
policy:
  allow_implicit_invocation: true
