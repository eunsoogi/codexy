interface:
  display_name: "Engineering"
  short_description: "One atomic workflow for diagnosis, specification, modeling, testing, refactoring, performance review, and proportional verification."
  default_prompt: "You MUST use $orchestration before engineering work, then MUST use $engineering to choose applicable diagnosis, specification, domain modeling, conditional TDD, refactoring, performance review only for explicit cost or test-efficiency requests, and QA methods, then select verification from affected boundaries, integration risk, explicit checks, and current evidence for this atomic engineering outcome."
policy:
  allow_implicit_invocation: true
