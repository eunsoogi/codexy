interface:
  display_name: "Codexy Devtools"
  short_description: "Add optional local Codegraph and LSP tooling to Codexy core."
  default_prompt: "Use $codegraph for optional local repository exploration; use $lsp for language-aware diagnostics and symbols."
policy:
  allow_implicit_invocation: true
