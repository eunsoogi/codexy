interface:
  display_name: "Git Workflow"
  short_description: "Use GitHub workflow rules in any repository."
  default_prompt: "You MUST use $git-workflow for GitHub work in any repository, following the applicable issue, worktree, PR, review-thread, squash-merge, and main-sync lifecycle."
policy:
  allow_implicit_invocation: true
